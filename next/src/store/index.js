/* eslint-disable no-unused-vars */
import { createStore } from "vuex";
import puples from "./modules/puples.js"

export default createStore({
  namespaced: true,
  modules: {

    puples,
    
    
  },
});
